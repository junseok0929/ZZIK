# 모아컷(MoaCut) — Codex 사전 개발 실행 프롬프트

이 문서를 읽고 현재 작업 공간에서 모아컷을 실제로 구현해줘. 기획 설명이나 코드 예시만 제시하지 말고, 파일 작성·실행·오류 수정·통합 검증까지 진행해줘.

## 1. 작업 맥락과 목표

나는 여행 사진을 함께 정리하고 보정본에 합의하는 서비스 **모아컷**을 해커톤 전에 미리 만들어보려고 한다.

기존 기획에 있던 1~5번 역할 분담과 M01~M17 머지 순서는 해커톤 당일 팀 협업을 위한 계획이다. 지금 개발에서는 그 역할이나 머지 순서를 기다릴 필요가 없다. 네가 전체 프로젝트를 통합 구현해줘. 하루짜리 해커톤이라는 이유로 처음부터 화면 목업만 만들거나 핵심 기능을 빼지 마.

목표는 다음 흐름이 실제로 이어지는 앱이다.

**공유 앨범 만들기 → 멤버 참여 → 사진 업로드 → 인물별 자동 정리 → 사진 선택 → 보정 버전 저장·비교 → 멤버 확인·승인 → 최종본 다운로드**

사진 자동 정리와 보정 합의가 모두 중요하다. 사용자는 개발자가 아닌 일반인이므로 화면에서는 Git 용어 대신 `새 보정본`, `이전 버전`, `확인 요청`, `최종본`처럼 이해하기 쉬운 표현을 사용해줘.

## 2. 개발 진행 원칙

1. 먼저 현재 저장소, `AGENTS.md`, 실행 환경, 기존 코드와 변경 사항을 확인해줘. 기존 프로젝트가 있으면 그 구조를 존중하고, 없으면 아래 기본 스택으로 시작해줘.
2. 짧은 구현 계획을 세운 뒤 바로 개발해줘. 일반적인 UI·파일 구조·라이브러리 선택은 합리적으로 결정하고 매번 나에게 확인받지 마.
3. 아래 **1차 완성 범위 전체**를 먼저 연결하고 검증해줘. 이어서 **2차 확장 범위**를 진행해줘. 단계는 개발 순서이며, 1차 화면을 만들었다는 이유로 작업을 끝내라는 뜻이 아니다.
4. 외부 계정이나 인증 정보가 없어도 로컬 앱·DB·파일 저장·보정·승인은 계속 구현해줘. 외부 연동은 실제 어댑터를 작성하고, 실행 확인이 안 된 부분만 구분해 기록해줘.
5. 상태만 바뀌는 가짜 버튼, 새로고침하면 사라지는 주요 데이터, 하드코딩한 분석 결과를 실제 AI처럼 표시하는 구현은 완료로 취급하지 마.
6. 기존 파일·사용자 변경·Git 이력을 보존해줘. Git은 실제 변경 단위로 관리하고, 요청받지 않은 원격 push나 공개 배포는 진행하지 마.
7. AWS 배포에 필요한 설정·예시·가이드는 작성하되, 이번 기본 실행 범위는 로컬 구현과 검증이다. 별도 지시 없이 유료 리소스를 새로 만들지 마.
8. 막힌 외부 기능 하나 때문에 나머지를 중단하지 마. 가능한 작업을 끝낸 후 남은 조건과 정확한 후속 명령을 알려줘.

## 3. 제품 정의

### 해결하려는 문제

친구 4명이 여행에서 사진을 각각 500장씩 찍으면 총 2,000장이 된다. 각자 나온 사진을 찾아 따로 전달하고, 비슷한 사진 중 잘 나온 사진을 고르고, 단체사진의 보정본을 다시 공유하며 의견을 받는 과정이 번거롭다.

모아컷은 사진을 한 앨범에 모은 뒤 각자 자기 사진을 쉽게 찾게 하고, 같은 사진의 여러 보정본을 원본을 보존하면서 비교·확정하도록 돕는다.

### 핵심 경험

- 내가 나온 사진은 `내 사진` 탭에서 바로 찾는다.
- A와 B가 함께 나온 사진은 두 사람의 목록에서 모두 찾을 수 있다.
- 누군가 보정해도 원본과 이전 보정본은 남는다.
- 이전 버전을 골라 내 취향의 새 버전을 만들 수 있다.
- 누가 어떤 보정본을 확인했는지 알 수 있다.
- 최종본 하나를 고르고 실제 파일로 내려받는다.

`2,000장 1분 처리`, `분류 정확도 100%`, `노동 제로` 같은 문구는 검증 없이 사용하지 마. 실제 처리 장수·시간·실패 수를 보여줘.

## 4. 기본 기술 구성

현재 작업 공간에 기존 구현이 없다면 아래 구성으로 시작해줘.

| 영역 | 기본 선택 | 목적 |
| --- | --- | --- |
| 프론트엔드 | React + TypeScript + Vite | 모바일에서도 쓰기 좋은 반응형 웹 |
| UI | Tailwind CSS, 필요한 최소 컴포넌트 라이브러리 | 일관된 화면과 접근성 |
| 서버 데이터 | TanStack Query 등 검증된 도구 | 조회·캐시 무효화·분석 상태 갱신 |
| 백엔드 | Python + FastAPI | 공통 API, 이미지 처리, AWS 연동 |
| DB | PostgreSQL + SQLAlchemy + Alembic | 실제 영속 저장과 마이그레이션 |
| 파일 저장 | 로컬 파일 어댑터 / AWS S3 어댑터 | 키 없이 로컬 실행, 이후 AWS 전환 |
| 얼굴 분석 | AWS Rekognition 어댑터 | 기준 인물 매칭과 다중 얼굴 분석 |
| 이미지 처리 | Pillow, 필요 시 OpenCV | 보정 렌더링·EXIF·품질 지표 |
| 분석 작업 | DB에 작업을 기록하는 별도 worker | 업로드 요청과 긴 분석 분리 |
| 실행 | Docker Compose 및 로컬 개발 명령 | 재현 가능한 실행 |
| 향후 배포 | Nginx Web EC2 / FastAPI App EC2 / RDS / S3 | 기존 3-tier 계획 연결 |

반응형 웹을 기본으로 선택한 이유는 기존 계획에 Web EC2와 Nginx가 있기 때문이다. 이미 네이티브 모바일 코드가 있다면 무조건 새 웹 앱으로 갈아엎지 말고 기존 환경에서 같은 사용자 흐름을 구현해줘.

패키지 버전은 현재 환경과 공식 문서를 확인해 호환되는 안정 버전으로 고정하고 lockfile을 남겨줘. Firebase와 AWS를 동시에 도입하거나 이미지 렌더링만을 위해 별도 Node 서버를 추가하지 마.

권장 폴더 구분은 `frontend/`, `backend/`, `infra/`, `docs/`다. 백엔드 안에서는 앨범·사진·인물 분석·보정·승인 모듈을 구분하되 별도 서비스 여러 개로 과도하게 나누지 마.

## 5. 1차 완성 범위 — 전체 흐름이 실제로 작동해야 함

### 5.1 계정·공유 앨범·멤버

- 최소한의 회원가입·로그인·로그아웃과 서버가 검증하는 세션을 구현해줘. 이메일 인증이나 소셜 로그인은 이번 필수 범위가 아니다.
- 비밀번호는 검증된 라이브러리로 해시하고, 세션 쿠키와 요청 검증을 적절하게 설정해줘.
- 앨범 생성·목록·상세와 초대 링크 또는 초대코드를 통한 참여를 구현해줘.
- 멤버와 사진 속 인물은 별도 개념으로 모델링해줘. 인물 프로필을 계정과 연결할 수 있어야 `내 사진`과 승인이 동작한다.
- 다른 사람의 인물 프로필 연결을 임의로 가로채지 못하도록 연결 확인 규칙을 둬.
- 조회·수정·다운로드·분석·승인 모두 앨범 멤버십을 서버에서 검증해줘.
- 작성자와 승인자는 클라이언트가 보내는 `user_id`가 아니라 인증된 세션으로 결정해줘.
- 로컬 데모용 계정은 제공하되, 계정을 바꾸는 개발용 기능이 실제 인증을 우회하는 운영 기능으로 남지 않도록 해줘.

### 5.2 기준 인물 등록

- 이름과 기준 얼굴 사진을 등록하고 대표 사진을 표시해줘.
- 얼굴이 없는 사진과 얼굴이 여러 개인 사진을 서로 다른 오류로 처리해줘.
- 1차 구현은 **멤버 얼굴을 한 번 등록한 뒤 사진 속 인물을 매칭하는 방식**이다. 등록 없는 자동 클러스터링이 완료되기 전에는 그렇게 안내해줘.
- 같은 앨범 안에서만 인물을 연결하고 다른 앨범의 얼굴을 검색 대상에 섞지 마.
- 인물 프로필·기준 사진 삭제와 관련 저장 데이터 정리 경로를 마련해줘.

### 5.3 다중 업로드·저장

- 여러 사진 선택, 드래그 앤 드롭, 미리보기, 선택 사진 제외를 구현해줘.
- JPEG·PNG부터 지원하고, 미지원 HEIC 등은 명확히 안내해줘. 확장자뿐 아니라 실제 이미지 디코딩·MIME·크기·픽셀 수를 검사해줘.
- 파일별 대기·업로드 중·성공·실패와 전체 진행률을 표시해줘.
- 파일을 한꺼번에 무제한 전송하지 말고 동시 업로드 수를 제한해줘. 성공분은 유지하고 실패분만 재시도할 수 있어야 해.
- 업로드 요청 식별자와 DB 제약 등으로 네트워크 재시도가 같은 사진을 중복 생성하지 않게 해줘. 같은 사진을 사용자가 의도적으로 다시 올리는 경우와 구분해줘.
- 원본 바이트는 보존하고, 썸네일과 분석용 파생 이미지는 별도로 만들어줘.
- S3 또는 로컬 파일 저장과 DB 저장 중 일부만 성공했을 때 복구·정리할 수 있어야 해.
- EXIF 방향을 통일한 파생 이미지를 분석·표시에 사용하고 좌표 기준을 문서화해줘.

### 5.4 비동기 분석·상태

- 업로드 완료 후 분석 작업을 등록하고 즉시 상태를 반환해줘. 사용자가 분석 완료까지 업로드 요청을 기다리지 않게 해줘.
- 기본 상태는 `pending → processing → completed / failed`로 두고 단계별 오류를 기록해줘.
- 파일 저장 성공과 분석 성공은 별도 상태다. 분석 실패 때문에 저장된 사진이 사라지면 안 돼.
- 작업을 DB에 기록하고 worker가 처리하도록 해줘. 동시 처리 제한, 외부 호출 timeout, 제한된 재시도, 중복 실행 방지, 종료된 worker의 작업 복구를 구현해줘.
- `FastAPI BackgroundTasks`나 메모리 큐만으로 영속 작업 큐가 구현됐다고 취급하지 마. 간단한 PostgreSQL 기반 worker부터 시작해도 된다.
- 실패한 사진만 다시 분석할 수 있어야 하고, 재분석 결과 저장은 중복 연결을 만들지 않아야 해.
- 화면은 polling 또는 SSE 중 단순하고 안정적인 방식으로 갱신해줘. 별도 WebSocket 서버는 필수가 아니다.

### 5.5 얼굴 분석·인물별 조회

- 얼굴 수, 얼굴별 위치, 매칭된 인물, 유사도, 미확정 얼굴 수, 분석 제공자와 모드를 저장해줘.
- `얼굴 없음`, `얼굴은 있지만 등록 인물과 불일치`, `분석 실패`를 구분해줘.
- `전체`, `내 사진`, `인물별`, `1인`, `2인 이상`, `얼굴 미검출`, `확인 필요` 필터를 제공해줘.
- A+B 조합 검색에서는 `둘 다 포함`과 `둘 중 누구든 포함`을 구분해줘.
- 두 얼굴이 감지됐다는 이유만으로 여행 멤버 전원이 나온 사진으로 표시하지 마.
- 여러 사람과 연결된 사진도 각 결과 목록에서는 한 번만 보여줘.
- 사용자가 인물 연결을 추가·제외할 수 있게 해줘. 수동 제외 기록도 남겨 재분석이 조용히 되돌리지 않도록 해줘.
- 얼굴 매칭 점수는 사진이 잘 나온 정도나 승인 여부로 사용하지 마.

### 5.6 사진 상세·기본 보정

- 확대 보기, 등장 인물, 분석 상태, 원본 다운로드를 구현해줘.
- 보정은 우선 밝기·채도부터 완성하고, 초기화·원본 보기·변경 미리보기를 제공해줘.
- 사용자 화면에는 이해하기 쉬운 슬라이더를 표시하고, 내부 값의 기본값·범위·변환식을 명시해줘.
- 원본을 덮어쓰지 않고 새 버전으로 저장해줘. 버전에는 작성자·시각·설정값·부모 버전·렌더러 버전을 남겨줘.
- 저장한 버전의 설정은 원본 기준 전체 설정값이다. 이전 버전의 JPEG에 다시 보정을 누적하지 마.
- 이전 버전을 선택해 새 버전을 만드는 기능으로 취향 분기를 지원해줘. 서로 다른 분기를 자동으로 합성하는 기능은 필요 없다.
- 목록에서 작성자·시간·변경 내용을 읽을 수 있고, 선택한 버전과 원본을 슬라이더로 비교할 수 있어야 해.

권장 설정 예시는 아래와 같다. 실제 UI 범위와 반드시 일치시켜줘.

```json
{
  "schema_version": 1,
  "renderer_version": "pillow-v1",
  "brightness": 1.0,
  "saturation": 1.0
}
```

### 5.7 비파괴 렌더링·다운로드

- 미리보기와 내보내기에 동일한 필터 정의·적용 순서·방향·색상 처리 규칙을 사용해줘.
- 기본 구현은 서버의 동일 렌더러로 미리보기와 최종 이미지를 생성해도 된다. 미리보기 요청은 debounce하고 오래된 응답이 최신 화면을 덮지 못하게 해줘.
- CSS 필터와 Pillow에 같은 숫자를 전달하면 색이 같다고 가정하지 마. 클라이언트 즉시 미리보기를 사용한다면 서버 결과와 차이를 검증해줘.
- 매번 원본에서 렌더링하고 최종 내보내기 시 결과 파일을 인코딩해줘. 승인한 버전 ID가 내보내기 요청에 명확하게 연결되어야 해.
- 원본 다운로드는 업로드한 원본 바이트를 반환하고, 보정본 다운로드는 해당 버전을 원본 해상도로 렌더링해줘.
- JPEG 출력은 인코딩 손실이 있으므로 `완전 무손실 보정`이라고 표시하지 마. 정확한 설명은 `원본 보존·반복 재압축 방지`다.
- 목록에는 썸네일을 사용하고, 원본 2,000장을 한 번에 브라우저 메모리에 올리지 마.
- 원본 해시와 버전 설정·렌더러 버전·출력 형식을 기준으로 렌더 캐시를 구분해줘.
- 로컬 파일 경로를 그대로 외부에 공개하지 말고, S3 모드에서는 비공개 객체와 짧은 만료시간의 서명 URL을 사용해줘.

### 5.8 승인·최종본

좋아요는 취향 표현이고, 승인은 특정 보정본에 대한 확인이다. 두 의미를 섞지 마.

기본 정책은 **그 사진에 나온 것으로 확인된 멤버 전원 승인**으로 구현해줘. 배경에 지나가는 사람이나 등록되지 않은 얼굴을 AI가 임의로 승인한 것으로 간주하면 안 된다.

아래 규칙을 서버에서 일관되게 적용해줘.

1. 승인 요청 전에 사람이 등장 멤버와 승인 대상 목록을 확인할 수 있게 해줘. AI 탐지만으로 게시 동의가 확보됐다고 표시하지 마.
2. 승인 대상은 버전별로 저장해줘. 대상이 없거나 계정과 연결되지 않은 대상이 있으면 자동으로 승인 완료 처리하지 마. 얼굴 없는 사진은 업로더를 기본 검토자로 명시해줘.
3. 버전 작성자는 본인이 승인 대상일 때만 승인 수에 포함되고, 새 버전은 항상 승인 0개에서 시작해줘.
4. 같은 사용자의 동일 버전 중복 승인은 DB 제약으로 막고 승인 취소를 지원해줘.
5. 모든 대상이 승인하면 해당 버전을 `합의 완료`로 표시해줘. 앨범 멤버가 합의 완료 버전 중 하나를 `최종본`으로 선택하도록 해줘.
6. 사진의 `final_version_id`는 하나만 존재한다. 새 버전을 만든다고 기존 최종본을 자동 교체하지 마.
7. 최종본에서 승인이 취소되어 조건을 잃으면 최종본 지정을 해제해줘. 다른 버전으로 조용히 바꾸지 마.
8. 인물 수정으로 승인 대상이 달라지면 기존 합의와 최종본을 재검토 상태로 전환하고, 변경된 대상을 가진 새 버전으로 다시 승인받게 해줘. 같은 보정값을 복사해도 승인은 복사하지 마.
9. 앨범에 새 멤버가 들어왔다고 기존 대상 목록을 자동 변경하지 마. 대상자의 탈퇴나 계정 연결 변경은 재검토 사유로 처리해줘.
10. 동시 승인·취소·최종본 지정에도 데이터가 모순되지 않도록 트랜잭션과 제약을 사용해줘.

## 6. AI 어댑터와 실행 모드

### 실제 분석

- 1차 실제 얼굴 분석은 Rekognition으로 구현해줘. ML Kit Face Detection만으로 동일 인물 식별이나 얼굴 임베딩 추출까지 된다고 가정하지 마.
- 소규모 등록 멤버 매칭은 `DetectFaces`와 기준 얼굴별 `CompareFaces` 조합으로 시작할 수 있다. `CompareFaces`의 source는 얼굴 하나인 기준 사진, target은 업로드 사진이다.
- 얼굴 없음은 탐지 결과로 먼저 처리해 불필요한 비교 오류를 피하고, 여러 인물의 결과를 대상 얼굴 위치와 연결해 중복·경합을 해결해줘.
- 유사도 임계값과 1·2위 후보 점수 차이 기준을 설정값으로 두고, 애매하면 미확정으로 남겨줘. 실제 검증 없이 특정 수치를 정확도 보장값으로 설명하지 마.
- 컬렉션 검색 방식으로 바꾼다면 `SearchFacesByImage`가 입력 이미지의 가장 큰 얼굴 하나를 검색한다는 점을 반영해줘. 단체사진의 모든 얼굴은 탐지 후 얼굴별 crop 검색 등의 절차가 필요하다.
- API 파일 크기·해상도·리전 조건은 현재 공식 문서를 확인해줘. 분석용 축소 이미지와 원본 저장 정책을 분리해줘.
- 인물 등록 수×사진 수에 따라 호출 수가 늘어날 수 있으므로 호출 수·처리 시간을 기록해줘. 무제한 재시도는 하지 마.

### AWS 키 없이 실행하는 방법

- `STORAGE_BACKEND=local|s3`, `FACE_ANALYSIS_PROVIDER=fixture|rekognition`처럼 실행 모드를 명시적으로 선택하게 해줘.
- fixture 모드에서는 지정된 샘플 파일의 해시에 연결된 고정 결과만 반환해줘. 임의의 새 사진에 랜덤 인물 이름을 붙이지 마.
- 지원하지 않는 사진은 `실제 분석 연결 필요`로 처리해줘. 샘플 분석에는 API와 화면 모두 `샘플` 표시가 있어야 해.
- 실제 분석 호출 실패를 fixture 성공으로 자동 대체하지 마.
- 모드가 달라도 API 응답 구조는 유지해줘. 분석 제공자·모드·실행 시각·모델 식별자 또는 API 버전을 가능한 범위에서 기록해줘.
- 실사진 검증은 사용자가 제공했거나 재사용이 허용된 사진으로 수행해줘. 확보하지 못했다면 AI 정확도 검증은 미실시로 기록하고 나머지 통합 검증을 계속해줘.

## 7. 2차 확장 범위 — 1차 검증 후 이어서 구현

이번 작업은 사전 개발이므로 아래 기능을 모두 `나중에`라고 미루지 말고 순서대로 진행해줘. 외부 모델·계정·데이터로 막힌 기능은 조건을 기록하고 다음 기능으로 넘어가줘. 미구현 기능을 동작하는 것처럼 노출하지 마.

### 7.1 장면·장소·촬영 시각

- 촬영 시각과 GPS를 읽고, 메타데이터가 없거나 깨진 경우도 처리해줘.
- 서버 저장 시간과 실제 촬영 시각을 구분하고, EXIF에 시간대가 없으면 임의로 확정하지 마.
- Rekognition 장면 라벨 등 실제 지원되는 분석으로 바다·산·음식·카페·야경 같은 태그를 생성해줘.
- 태그 기반 필터와 촬영일별 정리를 구현해줘.
- GPS가 있을 때만 역지오코딩으로 구체적인 장소명을 붙여줘. 외부 서비스가 없으면 좌표를 보존하고 장소명이 없다고 표시해줘.
- 이미지에 바다가 보인다는 이유로 특정 해변 이름을 지어내지 마.

### 7.2 유사 사진 그룹·베스트 컷

- 촬영 시각과 시각적 유사도를 함께 사용해 같은 순간의 후보 사진을 묶어줘. 촬영 시각이 없는 경우에는 보수적으로 그룹핑해줘.
- 흔들림·노출·가능한 경우 눈 감음 등으로 후보 1~3장을 추천해줘.
- Laplacian variance 같은 지표는 동일한 전처리 조건의 유사 사진 그룹 안에서 비교하고, 절대적인 미적 평가로 취급하지 마.
- 웃음 여부나 얼굴 매칭 점수를 아름다움 점수로 사용하지 마. 추천 이유는 `덜 흔들림`, `눈 감은 사람이 적음`처럼 관측한 신호로 설명해줘.
- 추천은 원본을 삭제하거나 숨기는 기능이 아니다. 사용자가 다른 사진을 고를 수 있어야 해.

### 7.3 등록 없는 자동 인물 그룹

- 등록 사진 없이도 앨범 안의 얼굴을 묶고 사람이 이름을 붙일 수 있는 별도 모드를 구현해줘.
- Rekognition 컬렉션을 이용한다면 앨범 단위로 검색 범위를 격리하고, face ID·그룹·사진의 대응 관계를 관리해줘.
- 후보 얼굴 검색, 같은 얼굴의 자기 매칭 제외, 약한 연결로 서로 다른 사람이 합쳐지는 문제를 처리해줘. 단순 연결만으로 무조건 그룹을 확장하지 마.
- 그룹 합치기·분리·잘못 연결된 사진 수정 경로를 제공해줘.
- Rekognition 내부 얼굴 벡터를 직접 내려받을 수 있다고 가정하지 마. 로컬 모델을 선택한다면 코드와 가중치의 이용 조건·다운로드·필요 자원을 확인해줘.
- 그룹을 사용자 계정과 연결하는 절차와, 그룹 수정 시 기존 승인 대상 재검토 정책을 1차 모델에 통합해줘.

### 7.4 자연어 검색

- `지수가 나온 바다 사진`, `둘이 찍은 사진`, `어제 찍은 음식 사진` 같은 한국어 입력을 지원해줘.
- 1차로 저장된 인물·태그·날짜를 조합하는 제한된 검색을 만들 수 있지만, 지원 문법과 한계를 표시해줘.
- 의미 기반 검색까지 구현한다면 한국어 지원을 확인한 이미지·텍스트 임베딩 모델을 실제로 연결해줘. 키워드 일치만 하는 기능을 의미 검색이라고 설명하지 마.
- 날짜 조건은 앨범 시간대·확인된 촬영 시각을 기준으로 하고, 최종 결과는 반드시 현재 앨범 권한 안에서 필터링해줘.

### 7.5 협업 편의 기능

- 사진 목적 라벨, 버전별 댓글·수정 요청, 다른 취향의 버전 이름을 구현해줘.
- `비공개` 표시를 제공한다면 누구에게 숨겨지는지 정의하고 서버 접근 권한까지 적용해줘. 단순 분류라면 `게시 제외`라는 이름을 사용해줘.
- `사진 선택 → 보정 중 → 확인 대기 → 확정` 상태를 볼 수 있는 간단한 보드를 제공해줘. 보정본 승인 상태와 모순되지 않게 파생 규칙을 정의해줘.
- 새 버전·승인 요청·수정 요청을 앱 안의 알림 목록으로 제공해줘. 저장 트랜잭션과 알림 전달을 분리해 알림 실패가 보정 저장을 취소하지 않도록 해줘.
- OS 푸시, 인스타그램 직접 게시, 피부·얼굴형 보정, 생성형 이미지 합성은 별도 후속 범위로 남겨도 된다.

## 8. 화면과 디자인 기준

사진이 중심인 깔끔한 한국어 앱으로 만들어줘. 관리자 대시보드처럼 표와 상태 숫자만 나열하지 마.

| 화면 | 필요한 경험 |
| --- | --- |
| 시작·로그인 | 서비스 한 줄 소개, 로그인, 샘플 앨범 체험 안내 |
| 앨범 목록 | 대표 사진, 앨범 이름, 멤버, 사진 수, 생성 버튼 |
| 앨범 상세 | 사진 격자, 인물 필터, 내 사진, 분석 진행, 업로드 |
| 업로드 패널 | 선택 사진, 제외, 파일별 진행·실패·재시도 |
| 사진 상세 | 큰 사진, 인물 수정, 보정·이력·승인·다운로드 |
| 보정 편집 | 넓은 미리보기, 밝기·채도, 초기화, 새 버전 저장 |
| 버전·승인 | 비교, 작성자·시각, 승인 대상과 진행, 최종본 선택 |
| 확장 화면 | 추천 후보, 인물 그룹 수정, 검색, 협업 보드 |

- 밝은 중성 배경과 절제된 초록 계열 포인트를 기본으로 사용해줘. 사진 색상과 경쟁하는 장식을 줄여줘.
- 데스크톱에서는 사진과 보정 패널을 나란히, 모바일에서는 전체 화면 사진과 하단 패널로 배치해줘.
- 로딩·빈 결과·권한 없음·처리 실패·네트워크 끊김을 각각 다뤄줘.
- 클릭 가능한 것처럼 보이는 요소는 실제 동작해야 하고, 미지원 기능은 이유를 표시해줘.
- 키보드 조작, 포커스, 버튼 레이블, 색 외의 상태 구분, 읽기 쉬운 대비를 챙겨줘.
- 사진 목록은 페이지네이션 또는 무한 스크롤을 사용하고, 썸네일 지연 로딩과 필요 시 가상화를 적용해줘.

## 9. 데이터·API 계약

테이블 이름을 그대로 강제하는 것은 아니지만 아래 개념은 분리해줘.

| 개념 | 보존할 내용 |
| --- | --- |
| 사용자·세션 | 인증 주체와 세션 유효성 |
| 앨범·멤버·초대 | 소유자, 참여자, 권한, 초대 상태 |
| 인물 | 앨범 범위의 인물, 기준 사진, 연결된 계정 |
| 사진 | 원본 위치·해시·크기·방향·촬영 정보·분석 상태 |
| 얼굴 탐지·인물 연결 | 얼굴 위치, AI 후보, 수동 추가·제외, 출처 |
| 분석 작업·실행 기록 | 제공자, 모드, 상태, 시도 횟수, 오류, 처리 시간 |
| 보정 버전 | 사진, 부모 버전, 작성자, 전체 보정값, 렌더러 버전 |
| 승인 대상·승인 | 버전별 대상 목록, 승인자, 승인·취소 상태 |
| 최종본 | 사진별 하나의 최종 버전 참조 |
| 파생 파일 | 썸네일, 미리보기, 내보내기 파일과 생성 기준 |
| 확장 데이터 | 태그, 후보 그룹, 추천 근거, 댓글, 알림 |

- 공통 API 오류에 안정적인 `code`, 사용자용 `message`, 필요 시 `details`를 포함해줘.
- 응답 타입과 프론트엔드 타입이 어긋나지 않도록 OpenAPI 또는 공유 계약을 활용해줘.
- 사진 목록은 pagination, 필터, 정렬을 지원하고 사진 수 집계의 기준을 일치시켜줘.
- 보정 버전 번호와 생성 시각은 서버에서 부여하고 동시 저장 충돌을 처리해줘.
- 다른 사진의 버전을 부모로 연결하거나 다른 앨범의 인물·멤버를 참조할 수 없게 해줘.
- 승인 요청을 시작한 버전의 보정값·승인 대상은 불변으로 취급하고 변경 시 새 버전을 만들어줘.
- 사진 삭제·앨범 삭제·인물 삭제에 따른 파일과 분석 데이터 정리 규칙을 마련하고 사용자에게 영향을 보여줘.

## 10. 실행·설정·AWS 전환

`README.md`의 명령대로 새 환경에서 실행할 수 있게 해줘.

- `.env.example`, 의존성 lockfile, 마이그레이션, Dockerfile, `compose.yaml`을 제공해줘.
- 앱·worker·DB가 함께 실행되고 재시작 후 데이터가 유지되도록 볼륨을 설정해줘.
- 마이그레이션은 중복 실행에 안전한 명령으로 제공하고, worker와 API가 동시에 스키마를 바꾸지 않게 해줘.
- 로컬 기본값은 PostgreSQL과 로컬 파일 저장이다. S3와 Rekognition은 명시적으로 활성화해줘.
- AWS 자격증명은 표준 SDK 인증 체계와 EC2 역할을 사용하고 프론트엔드·저장소에 넣지 마.
- Nginx 정적 파일 서빙, SPA 새로고침, `/api` 프록시, 업로드 크기, 적절한 timeout 예시를 작성해줘.
- App 실행·worker 실행·환경변수 주입·상태 확인·로그 확인·배포·롤백 명령을 문서화해줘.
- Web → App → RDS 접근 관계와 비공개 S3 설정을 기존 3-tier 기준으로 설명해줘. S3와 Rekognition은 앱에서 사용하는 외부 서비스로 구분해줘.
- 헬스체크는 단순 프로세스 생존과 DB 등 준비 상태를 구분해줘. 로컬 모드에서 미설정 AWS가 전체 서버 시작을 막지 않게 해줘.

## 11. 구현 순서와 중간 기록

아래 순서로 개발하되 기존 코드 상태에 맞게 세부 순서는 조정해도 된다.

1. 저장소 확인, 실행 기반, 데이터 모델, API 계약, 기본 화면.
2. 인증·앨범 참여·업로드·실제 파일 저장·원본 조회.
3. 분석 worker·fixture·실제 Rekognition 어댑터·인물 필터·수동 수정.
4. 보정 렌더러·버전 저장·이력·비교·보정본 다운로드.
5. 승인 대상·승인·취소·최종본·다중 사용자 통합 검증.
6. 장면·장소·추천 → 자동 인물 그룹 → 검색 → 협업 편의 기능.
7. UI 마무리, 최종 검증, 실행·AWS 전환 문서 정리.

`docs/IMPLEMENTATION_STATUS.md`에 기능별 `구현·검증 완료`, `코드 구현/실연동 미검증`, `샘플 전용`, `미구현/차단 사유`를 구분해 기록해줘. 작업이 길어져도 이 파일과 실제 코드를 기준으로 이어서 진행해줘.

병렬 작업 도구가 있다면 보정 UI, AI 어댑터, 공통 API처럼 경계가 명확한 부분을 나눠 진행해도 된다. 공통 데이터 계약과 마이그레이션은 충돌 없이 통합하고 최종 검증은 전체 앱 기준으로 해줘.

## 12. 완료 검증

단위 테스트를 많이 만드는 것보다 실제 사용자 흐름과 깨지기 쉬운 경계를 검증해줘. 테스트 결과를 만들어내거나 실행하지 않은 항목을 통과라고 쓰지 마.

### 필수 시나리오

- [ ] 서로 다른 두 계정이 같은 앨범에 참여하고 각자 사진을 올릴 수 있다.
- [ ] 참여하지 않은 계정은 사진 조회·다운로드·보정·승인을 할 수 없다.
- [ ] 새로고침과 서버 재시작 후 앨범·사진·버전·승인 데이터가 유지된다.
- [ ] 여러 사진 중 실패한 파일만 재시도하며 성공분은 중복 생성되지 않는다.
- [ ] 업로드 성공과 분석 실패가 구분되고 실패한 분석만 재시도할 수 있다.
- [ ] worker 중단 후 멈춘 작업을 복구하며 동일 결과가 중복 저장되지 않는다.
- [ ] 얼굴 없음·미등록 얼굴·복수 인물·분석 실패를 구분한다.
- [ ] A+B 사진은 A와 B 목록에 모두 나타나고 각 목록 안에서는 중복되지 않는다.
- [ ] 수동 인물 추가·제외가 재분석 후에도 정해진 규칙대로 보존된다.
- [ ] 밝기·채도가 다른 버전 두 개를 저장하고 다시 접속해 재현한다.
- [ ] 이전 버전에서 새 버전을 만들며 기존 버전은 그대로 남는다.
- [ ] 원본의 파일 해시가 보정 전후·다운로드 후 동일하다.
- [ ] 미리보기와 출력물의 방향·종횡비·보정 효과가 일치하고, 출력 해상도는 원본과 일치한다.
- [ ] 버전 A 승인 후 버전 B를 만들면 B는 승인 0개이고 A의 승인은 유지된다.
- [ ] 승인 중복, 승인 취소, 승인 대상 변경, 최종본 해제 규칙이 동작한다.
- [ ] 두 계정의 동시 요청에도 승인 수와 최종본이 모순되지 않는다.
- [ ] 원본·선택 버전·최종본 다운로드가 구분되고 올바른 파일을 받는다.
- [ ] fixture 결과는 샘플로 표시되고 실제 AI 오류가 가짜 성공으로 바뀌지 않는다.
- [ ] 모바일과 데스크톱에서 핵심 흐름을 수행하고 가로 넘침·깨진 모달이 없다.

자동 검증은 핵심 서비스 로직과 API를 pytest 등으로, 핵심 브라우저 흐름을 Playwright 등으로 수행해줘. 서로 다른 사용자 세션을 사용하고 DB를 실제로 거치는 통합 검증을 포함해줘.

실제 AI 성능은 별도 검증표로 남겨줘. 검증 사진별 정답 인물·예측 인물·누락·오매칭·처리 시간을 기록하고, fixture 테스트와 혼합하지 마. 실제 사진이나 AWS 설정이 없으면 미검증으로 표시해줘.

## 13. 최종 산출물과 보고 방식

최소한 다음 산출물을 남겨줘.

- 실행 가능한 프론트엔드·백엔드·분석 worker 코드.
- DB 마이그레이션, 환경변수 예시, 실행 구성, 의존성 lockfile.
- `README.md`: 가장 빠른 로컬 실행·샘플 체험·실제 AWS 연결 방법.
- `docs/ARCHITECTURE.md`: 구성, 데이터 흐름, 주요 설계 결정.
- `docs/IMPLEMENTATION_STATUS.md`: 실제 기능 상태와 검증 결과.
- `docs/DEMO_GUIDE.md`: 두 사용자로 업로드부터 최종본 다운로드까지 시연하는 순서.
- `docs/AWS_DEPLOYMENT.md`: 기존 3-tier에 옮기는 설정·절차·롤백·정리 방법.
- 핵심 테스트와 실제 실행 결과. 사진 자산을 포함했다면 출처·이용 조건.

최종 응답은 다음 순서로 짧고 정확하게 작성해줘.

1. 실제 구현한 기능.
2. 내가 바로 실행할 정확한 명령과 접속 주소.
3. 수행한 검증과 결과.
4. 실제 AI 연결 여부와 샘플로 동작하는 부분.
5. 남은 기능·차단 사유·내가 입력해야 할 설정.

계획만 설명하고 멈추지 말고, 지금 저장소 확인부터 시작해서 구현과 검증까지 진행해줘.

## 참고: 구현 시 확인할 공식 문서

아래 자료는 기술적 혼동을 방지하기 위한 참고다. 실제 사용하는 SDK 버전의 상세 조건은 구현 시 다시 확인해줘.

- [ML Kit Face Detection — 얼굴 탐지와 인물 식별의 구분](https://developers.google.com/ml-kit/vision/face-detection)
- [AWS CompareFaces — source 얼굴과 target 얼굴 비교](https://docs.aws.amazon.com/rekognition/latest/APIReference/API_CompareFaces.html)
- [AWS SearchFacesByImage — 입력 이미지의 가장 큰 얼굴 검색](https://docs.aws.amazon.com/rekognition/latest/APIReference/API_SearchFacesByImage.html)
- [AWS IndexFaces — 얼굴 특징 저장 및 접근 범위](https://docs.aws.amazon.com/rekognition/latest/dg/add-faces-to-collection-procedure.html)
- [FastAPI Background Tasks — 사용 범위와 별도 작업 처리 고려](https://fastapi.tiangolo.com/tutorial/background-tasks/)
- [Pillow ImageEnhance — 밝기와 색상 보정 계수](https://pillow.readthedocs.io/en/stable/reference/ImageEnhance.html)
